export interface Repository {
  id: number;
  name: string;
  language: string;
}

export interface UserDetails {
  id: number;
  avatar_url: string;
  login: string;
  name: string;
  bio: string;
}
